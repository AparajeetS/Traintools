# Security Policy

`traintools` is a local Python library for ML training diagnostics. It should not
execute remote code, open network connections during normal use, or collect
training data.

Please report suspected security issues through GitHub issues if no private
security advisory channel is available yet. Include the affected version, a
minimal reproduction, and whether the issue can expose model weights, data, or
environment variables.
